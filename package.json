{
  "name": "playlist-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build && cd .."
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.4"

  }
}
